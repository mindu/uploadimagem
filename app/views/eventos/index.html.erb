<h1>Listing eventos</h1>

<table border="0">
  <tr>
    <th>Data</th>  	
    <th>Titulo</th>
    <td>&nbsp;</td>
  </tr>

<% for evento in @eventos %>
  <tr>
		<td><%=h evento.data.strftime("%d/%m/%Y") %></td>  	
    <td><%=h evento.titulo %></td>
    <td><%= link_to 'Show', evento %></td>
  </tr>
<% end %>
</table>
<br />
Tag Cloud:
<br />
<% tag_cloud @tags, %w(nube1 nube2 nube3 nube4 nube5) do |name, css_class| %>
  <%= link_to name, {:controller => 'tags', :id => name},
    :class => css_class %>
<% end %>
<br />